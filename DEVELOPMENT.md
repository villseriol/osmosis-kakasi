# Development

## Debian Reference

```sh
# install prerequisites
sudo apt install dh-make debhelper dpkg-sig debsig-verify

# debian initialization command
dh_make --native -i -p osmosis-kakasi_1.0.0 -e ey4180@villseriol.org -c gpl3
```

## Maven Reference
